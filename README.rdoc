== README

OpenDSA Student Data Aggregation

Startup Guide:
```sh
bundle install
rake db:migrate
rake db:seed
rails s
```

Now your application should be running on port 3000.

<tt>rake doc:app</tt>.
